<?php

/**
 * Implements hook_uninstall().
 */
function aichatbot_uninstall() {
  // Clear the custom cache bin safely.
  \Drupal::service('cache_factory')->get('custom_aichatbot')->deleteAll();
  \Drupal::logger('aichatbot')->notice('custom_aichatbot cache deleted');
}
